# Copy to .env and fill values for Internet use
LRCOM_DOMAIN=your-domain.com

# Web HTTPS endpoint is https://<public-host>:8443

# TURN must be reachable by browsers (NOT localhost)
LRCOM_TURN_HOST=your.public.domain.or.ip

# Optional STUN server(s), comma-separated. If unset, LRcom:
# - uses NO default STUN when TURN is configured (privacy-friendly)
# - otherwise falls back to a public STUN server for usability
#
# If you run coturn, it can usually also serve STUN on :3478.
#STUN_URLS=stun:your.public.domain.or.ip:3478

# Strong shared secret (same for lrcom + coturn)
LRCOM_TURN_SECRET=change-me-to-a-long-random-string

# Often required when coturn is behind NAT/Docker
# Set to your public IP (router WAN IP)
LRCOM_TURN_EXTERNAL_IP=your.public.ip

# Relay port range (keep <= 10 ports if needed)
LRCOM_TURN_MIN_PORT=49160
LRCOM_TURN_MAX_PORT=49169

# TLS SANs for the self-signed cert (optional)
# Add your public host/IP if you access by IP/hostname
LRCOM_TLS_SANS=DNS:localhost,IP:127.0.0.1

# Optional: Web Push for background notifications (incoming calls/messages)
# If unset, LRcom still supports in-tab notifications when the page is open.
#
# Generate keys locally:
#   npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:lrcom@localhost

# Optional: change the visual app name (header + setup title only)
LRCOM_APP_NAME=Last

# Optional: enable server debug logs (true/false), defaulf false
DEBUG=false

# Optional: Voice call audio quality settings
# Echo cancellation (true/false, default: true)
#VITE_AUDIO_ECHO_CANCELLATION=true
# Noise suppression (true/false, default: true)
#VITE_AUDIO_NOISE_SUPPRESSION=true
# Auto gain control (true/false, default: true)
#VITE_AUDIO_AUTO_GAIN=true
# Sample rate in Hz (8000-96000, default: browser default ~48000)
#VITE_AUDIO_SAMPLE_RATE=48000
# Opus codec max bitrate in kbps (6-510, default: browser default ~32-64)
#VITE_AUDIO_MAX_BITRATE=64

ACME_EMAIL=you@domain.com


POSTGRES_DB=test
POSTGRES_USER=testuser
POSTGRES_PASSWORD=testpassword